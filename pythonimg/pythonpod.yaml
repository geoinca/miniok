apiVersion: v1
kind: Pod
metadata:
  name: pytest
  labels:
    app: pytest
spec:
  containers:
  - name: pytest
    image: geoincaks/pythonfacealignment:0.1.3
    command: ["/bin/sleep", "3650d"]
    imagePullPolicy: IfNotPresent
  restartPolicy: Always